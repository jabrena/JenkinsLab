pipeline {
    agent {
        //docker { image 'node:7-alpine' }
        any
    }
    stages {
        stage('Test') {
            steps {
                bat 'node --version'
            }
        }
    }
}
